venv/
__pycache__/
*.pyc
.DS_Store

# Benchmark artifacts: the plot is committed as a reference rendering of the
# synthetic sweep; the raw metric dump is regenerated by every run.
benchmark_results.json

# HotpotQA parquet: 27 MB, downloaded on demand by
# experiments/final_validation/data.py, SHA-256 recorded in the run artifacts.
data/
